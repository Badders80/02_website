# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment files (NEVER commit secrets)
.env
.env.*
!.env.local.example

# Consolidated local vault (moved dumps live here)
.secrets/

# Vercel
.vercel/

# TypeScript
tsconfig.tsbuildinfo

# OS/IDE
.DS_Store
.vscode/
Thumbs.db
.chrome-profile/

# Working copies / dumps that used to litter the root
_reference/
temp_key.json
website-api-key.json
vercel-env.json
*api-key*.json
*service-account*.json

# Audit working files (not for repo)
evolutionstables.nz-audit/

# Relay working files
relay/inbox/
relay/outbox/

# Google credentials — never commit
scripts/firebase-service-account.json
scripts/client-secret.json
scripts/token.json
scripts/*.pem

# Image backups — never commit raw backups
public/images/images_backup/
