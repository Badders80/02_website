Let me review this design system implementation plan carefully. I need to
check for technical feasibility, architecture gaps, token system issues,
execution order problems, risk areas, and missing items.

Let me analyze each aspect:

## 1. TECHNICAL FEASIBILITY

### Tailwind v3 + Next.js 16 + React 19 compatibility:

**Issues I see:**

1. **`tailwind.config.ts`** - The plan shows adding colors to `theme.extend
`theme.extend.colors` but the syntax shown is:
```typescript
        // Supplementary neutrals (Better Stack)
        "muted-steel": "var(--muted-steel)",
        "steel-border": "var(--steel-border)",
        "frost": "var(--frost)",
        "pure-white": "var(--pure-white)",
```
This needs to be inside a `colors` object. The plan doesn't show the full s
structure. In Tailwind v3, it should be:
```typescript
theme: {
  extend: {
    colors: {
      "muted-steel": "var(--muted-steel)",
      // ...
    }
  }
}
```

2. **`maxWidth` in Tailwind config** - The plan shows:
```typescript
      maxWidth: {
        page: "var(--max-width-page)",
        narrow: "var(--max-width-narrow)",
      },
```
This should be under `theme.extend.maxWidth` not at root level.

3. **Container component** - Uses `max-w-page` and `max-w-narrow` classes.
These need to be generated by Tailwind from the config. The config needs to
to be correct.

4. **Modal component** - Uses `createPortal` from `react-dom`. In React 19,
19, `createPortal` is still available from `react-dom`. This should work.

5. **Modal component** - Uses `animate-fade-in` class. Need to verify this
animation exists in globals.css. The plan mentions "animations" in tailwind
tailwind.config.ts but doesn't show the fade-in keyframe.

6. **LogoCarousel** - Uses `animation: logo-scroll ${speed}s linear infinit
infinite` inline style. This works but the keyframe needs to be in globals.
globals.css (which the plan adds).

7. **Input/Select/Textarea primitives** - The plan creates separate files.
In shadcn/ui pattern, these are often combined. But separate files work fin
fine.

8. **Badge component** - Uses `cva` (class-variance-authority). The variant
variants reference colors like `success`, `warning`, `error`, `success-text
`success-text`, `warning-text`, `error-text` but these aren't defined in th
the token system. The plan only defines `accent` as a color token. This wil
will break unless those colors are added.

9. **Select component** - The plan creates a basic `<select>` wrapper. But
in modern React apps, people often use headless UI libraries like `@radix-u
`@radix-ui/react-select` or `@headlessui/react`. A native select has limite
limited styling capabilities (can't style the dropdown arrow consistently a
across browsers). This might be a UX issue but technically works.

10. **Tabs component** - Uses React Context. This is fine for React 19.

11. **GlowPillButton tokenization** - The plan shows replacing `via-primary
`via-primary` with `via-accent` but `primary` isn't defined in the token sy
system. The token is `accent` (gold). Need to check if `primary` exists in
current tailwind config.

12. **ComingSoonOverlay tokenization** - Uses `bg-canvas/40` and `border-bo
`border-border`. These need to be in Tailwind config.

13. **LogoCarousel** - Uses `Image` from `next/image` with `fill` prop. The
The `fill` prop makes the image absolute positioned. The parent needs `rela
`relative` and explicit dimensions. The plan has `style={{ height: logoHeig
logoHeight }}` on the parent div but no width. With `fill`, the image will
fill the parent. The parent has `shrink-0` but no width constraint. This mi
might cause layout issues.

14. **LogoCarousel** - The `loopLogos` duplication for seamless loop is cor
correct, but the animation moves `-50%` which assumes the duplicated set eq
equals the original width. This works if all logos have same width, but the
they don't (different aspect ratios). The gap-12 adds spacing. This is a kn
known issue with logo carousels - they need fixed-width items or the animat
animation calculation needs to account for variable widths.

### Next.js 16 specific:
- Next.js 16 uses React 19
- App Router is stable
- `createPortal` works the same
- `next/image` fill prop works

### React 19 specific:
- `forwardRef` works the same
- Context works the same
- No breaking changes for these patterns

## 2. ARCHITECTURE GAPTS

### Missing primitives:
1. **Button primitive** - The plan mentions `button.tsx` exists (shadcn but
button). But the login page rebuild task says "Replace GlowPillButton submi
submit with Button primitive (accent variant for primary action)". The exis
existing button has variants: default, outline, secondary, ghost, destructi
destructive, link. It doesn't have an "accent" variant. The plan needs to e
either add an accent variant to the existing button or clarify which varian
variant to use.

2. **Label primitive** - For forms, a Label component is useful for accessi
accessibility. Not in the plan.

3. **Separator/HR primitive** - Used in modals, cards, etc. Not in the plan
plan.

4. **Avatar primitive** - Might be needed for user profiles in mystable.

5. **Dropdown/Menu primitive** - NavBar likely has dropdowns. Not in the pl
plan.

6. **Tooltip primitive** - Might be needed.

7. **Sheet/Slide-over** - Listed as "missing" in current state but not buil
built in Phase 2. The plan says "No Sheet/Slide-over primitive — no mobile
slide-over pattern" but doesn't include it in Phase 2 tasks.

8. **Table primitive** - Listed as missing (PedigreeTable is raw HTML). Not
Not built in Phase 2. The plan says to tokenise PedigreeTable in Phase 3 bu
but not create a reusable Table primitive.

9. **Checkbox/Radio/Switch primitives** - Forms in PurchaseFlow, KYC, login
login might need these.

10. **Alert/Toast primitive** - For notifications.

### Abstraction level issues:
- The Card primitive includes CardHeader, CardTitle, CardDescription, CardC
CardContent, CardFooter. This is good shadcn pattern.
- The Modal includes ModalHeader, ModalBody, ModalFooter. Good.
- The Tabs includes TabsList, TabsTrigger, TabsContent. Good.
- But Input, Textarea, Select are separate files without a shared FormField
FormField wrapper for labels, errors, descriptions.

## 3. TOKEN SYSTEM

### Warm/Cool neutral mix:
The plan keeps warm greyscale as base and adds 4 Better Stack cool neutrals
neutrals as supplements. The DESIGN.md says:
> "Don't: Mix warm and cool neutrals in the same component — pick one per c
context"

This is a good rule but needs clear guidance on which context uses which. T
The plan doesn't specify:
- When to use `text-muted-foreground` (warm, #737373) vs `text-muted-steel`
`text-muted-steel` (cool, #646e87)
- When to use `border-border` (warm, rgba(255,255,255,0.1)) vs `border-stee
`border-steel-border` (cool, #939db8)
- When to use `text-frost` (cool, #c9d3ee) vs `text-heading` (warm, #f8fafc
#f8fafc) vs `text-pure-white` (#ffffff)

### Elevation system:
Three layers: canvas (#030303), surface (#0a0a0a), raised (#111111).
- Canvas → Surface: +7 in hex (barely perceptible)
- Surface → Raised: +7 in hex (barely perceptible)

These are very subtle differences. On many monitors, especially non-OLED, t
these may be indistinguishable. Better Stack uses more contrast between ele
elevations. This could be a visual issue.

### Contrast/WCAG concerns:
- `text-foreground` = rgba(255,255,255,0.5) on `bg-surface-base` (#0a0a0a):
(#0a0a0a): contrast ratio ~ 7.5:1 (passes AAA for large text, AA for normal
normal)
- `text-muted-foreground` = #737373 on `bg-surface-base` (#0a0a0a): contras
contrast ratio ~ 4.5:1 (passes AA for large text only)
- `text-muted-steel` = #646e87 on `bg-surface-base` (#0a0a0a): contrast rat
ratio ~ 3.8:1 (fails AA for normal text)
- `border-border` = rgba(255,255,255,0.1) on `bg-surface-base`: barely visi
visible
- `border-steel-border` = #939db8 on `bg-surface-base`: contrast ~ 3.5:1 (f
(fails AA for UI components which need 3:1)

The cool neutrals have lower contrast on the dark warm backgrounds. This is
is a significant accessibility risk.

### Missing color tokens for Badge variants:
The Badge component uses: `success`, `warning`, `destructive`, `success-tex
`success-text`, `warning-text`, `error-text`. None of these are defined in
the token system. The plan only defines `accent` (gold). This will break th
the build.

## 4. EXECUTION ORDER

### Dependency issues:
1. **Task 1 (globals.css tokens)** → **Task 2 (tailwind config)** → **Task
6 (Container)** → **Task 7 (Card)** → **Task 8 (Input)** etc. This order is
is correct.

2. **Task 11 (GlowPillButton tokenize)** depends on Tailwind config having
`accent` color. The plan says "replace `via-primary` with `via-accent`". Bu
But `primary` might not exist. Need to check current config.

3. **Task 17 (LogoCarousel)** adds keyframe to globals.css. This should be
before Task 18 (integrate).

4. **Task 19 (CampaignStatusBadge)** depends on Badge primitive (Task 7). C
Correct order.

5. **Task 20 (KycBadge)** depends on Badge primitive. Correct.

6. **Task 21.5 (login page rebuild)** depends on Card, Input, Button primit
primitives. These are built in Phase 2. Correct.

7. **Phase 3 (surface system)** depends on tokens and primitives. Correct.

8. **Phase 5 (sweep)** depends on everything. Correct.

### Potential reorder:
- Task 3 (dot-grid utility) could be done anytime after Task 1.
- Task 4 (elevation tokens) could be combined with Task 1.
- Task 5 (DESIGN.md) could be done earlier as reference.

## 5. RISK AREAS

### High risk files:
1. **src/app/mystable/page.tsx** - "60+ instances in PurchaseFlow alone". L
Large file, many ad-hoc values. High chance of missing some or breaking lay
layout.

2. **src/components/marketplace/PurchaseFlow.tsx** - Complex logic + stylin
styling. Tokenizing might break interactive states.

3. **src/components/marketplace/DetailTabs.tsx** - Custom tab logic. Replac
Replacing with Tabs primitive (Task 10) changes behavior. Need to ensure fe
feature parity.

4. **src/components/marketplace/PedigreeTable.tsx** - Raw HTML table. Conve
Converting to tokenized classes might break table layout.

5. **src/components/NavBar.tsx** - Appears on every page. Mobile menu, drop
dropdowns, scroll effects. High risk.

5. **src/app/auth/login/page.tsx** - Complete rebuild. New component struct
structure. High risk of accessibility regressions.

### force-dynamic pages:
- marketplace/[id]/page.tsx and mystable/page.tsx are force-dynamic. The to
tokenization doesn't affect this, but any layout shift during tokenization
could affect loading performance.

### Existing animations:
- The plan says "keep existing animations". But some animations might use h
hardcoded colors (e.g., shimmer on cards). Need to verify.

### Client components:
- Modal, Tabs, LogoCarousel, GlowPillButton are "use client". The primitive
primitives need to be client components if they use state/effects. Card, Ba
Badge, Input, Container can be server components. The plan doesn't specify
"use client" for Card, Badge, Input - but they don't need it.

## 6. MISSING ITEMS

1. **Success/Warning/Error color tokens** - Required for Badge variants. No
Not in plan.

2. **Button accent variant** - Login page rebuild needs it. Not in plan.

3. **Sheet/Slide-over primitive** - Listed as missing, not built.

4. **Table primitive** - PedigreeTable needs it.

5. **FormField/Label primitive** - For accessible forms.

6. **Dropdown/Menu primitive** - NavBar needs it.

7. **Checkbox/Radio/Switch** - Forms need them.

8. **Toast/Alert primitive** - For notifications.

9. **Avatar primitive** - User profile.

10. **Separator primitive** - Common UI element.

11. **Tooltip primitive** - Might be needed.

12. **Scrollbar styling** - TabsList uses `scrollbar-none` but custom scrol
scrollbar might be needed.

13. **Focus visible ring tokens** - Input uses `focus-visible:ring-accent`.
`focus-visible:ring-accent`. Need to ensure ring colors are tokenized.

14. **Disabled state tokens** - Input has `disabled:opacity-50`. Should be
tokenized.

15. **Placeholder color token** - Input uses `placeholder:text-muted-foregr
`placeholder:text-muted-foreground`. OK.

16. **LogoCarousel variable width issue** - The seamless loop animation ass
assumes equal-width items. Real logos have different aspect ratios. This is
is a known hard problem. The plan doesn't address it.

17. **Image optimization for LogoCarousel** - Using `next/image` with `fill
`fill` on many logos might cause layout shift or performance issues. Should
Should use `priority` for first few? Or use regular `<img>` with proper siz
sizing.

18. **Design.md lint** - The plan says `npx -y @google/design.md lint DESIG
DESIGN.md`. Need to verify this package exists and works.

19. **Migration of existing shadcn components** - The project has `componen
`components.json` with shadcn config. The plan builds new primitives but do
doesn't mention migrating existing shadcn components (Button exists, maybe
others).

20. **Storybook or visual regression testing** - Not mentioned. Hard to ver
verify 75 files visually without tooling.

21. **Dark mode only** - The site is dark-only. The plan doesn't need light
light mode tokens, but should document this.

22. **Print styles** - Not mentioned.

23. **RTL support** - Not mentioned (probably not needed for NZ).

24. **Reduced motion** - Animations (logo-scroll, fade-in) should respect `
`prefers-reduced-motion`.

25. **TypeScript strictness** - The component props interfaces look good bu
but some use `React.HTMLAttributes` which includes `ref`. ForwardRef handle
handles this.

---

Now let me formulate the verdict.
...done thinking.

## VERDICT: NEEDS REVISION

The plan has solid architectural direction but contains several critical te
technical gaps that will cause build failures, accessibility violations, an
and runtime bugs if executed as-written.

---

### Critical Issues (must fix before execution)

#### 1. Missing color tokens for Badge primitive — **build will fail**
The `badge.tsx` (Task 7) references `success`, `warning`, `destructive`, `e
`error`, `success-text`, `warning-text`, `error-text` color tokens. **None
exist** in the token system. The plan only defines `accent` (gold) + 4 cool
cool neutrals + base warm greyscale.

**Fix:** Add semantic status colors to `globals.css` + `tailwind.config.ts`
`tailwind.config.ts`:
```css
/* Status colors (semantic, not decorative) */
--color-success: #22c55e;
--color-success-text: #dcfce7;
--color-warning: #f59e0b;
--color-warning-text: #fef3c7;
--color-error: #ef4444;
--color-error-text: #fee2e2;
```
Map in Tailwind: `success`, `success-text`, `warning`, `warning-text`, `err
`error`, `error-text`.

#### 2. Button primitive lacks "accent" variant — **login rebuild blocked**
blocked**
Task 21.5 says "Replace GlowPillButton submit with Button primitiv
primitive (accent variant)". The existing `button.tsx` (shadcn) has: `defau
`default`, `outline`, `secondary`, `ghost`, `destructive`, `link`. **No `ac
`accent` variant exists.**

**Fix:** Add `accent` variant to `button.tsx` before Task 21.5:
```typescript
accent: "bg-accent text-pure-white hover:bg-accent-hover border-accent",
```

#### 3. Cool neutrals fail WCAG contrast on warm dark surfaces
| Token | Value | On `#0a0a0a` (surface) | WCAG AA (4.5:1 text, 3:1 UI) |
|-------|-------|------------------------|------------------------------|
| `muted-steel` | `#646e87` | **3.8:1** | ❌ FAIL (text) |
| `steel-border` | `#939db8` | **3.5:1** | ❌ FAIL (UI border) |
| `border-border` (warm) | `rgba(255,255,255,0.1)` | ~1.1:1 | ❌ FAIL (UI b
border) |

**Fix:** Either darken cool neutrals (`#7a85a0`, `#a8b0ca`) or lighten surf
surface elevations. Document the contrast reality in DESIGN.md — don't clai
claim compliance.

#### 4. LogoCarousel seamless loop broken for variable-width logos
The animation `translateX(-50%)` assumes duplicated set = original width. R
Real partner logos have **different aspect ratios** (Trackside NZ vs Busine
Business Desk vs Investing.com). The loop will jump visibly.

**Fix options:**
- Constrain logos to fixed height + max-width, accept variable width, use J
JS-based infinite scroll (IntersectionObserver + append/prepend)
- Or: render logos in a `<marquee>`-style CSS animation but calculate total
total width via `resizeObserver` and set `--track-width` custom property
- Simplest: use `embla-carousel-react` (already in deps?) or a lightweight
infinite scroller library

#### 5. `tailwind.config.ts` syntax errors in plan
The plan shows color/maxWidth additions at wrong nesting level:
```typescript
// WRONG - shown in plan
maxWidth: { page: "var(--max-width-page)" }

// CORRECT - Tailwind v3
theme: {
  extend: {
    maxWidth: { page: "var(--max-width-page)" }
  }
}
```
Same for colors — must be under `theme.extend.colors`. The plan's snippets
will cause "Unknown utility class" errors.

#### 6. `Modal` uses `animate-fade-in` but keyframe not guaranteed
Task 9 uses `animate-fade-in` class. The plan mentions "animations, keyfram
keyframes" in tailwind config but doesn't show the keyframe definition. If
missing, animation does nothing (silent fail).

**Fix:** Verify `@keyframes fade-in` exists in `globals.css` or add it.

#### 7. `Select` primitive — native `<select>` cannot be styled consistentl
consistently
The dropdown arrow, option background, and hover states are browser-control
browser-controlled. On dark theme, options render as white-on-white in Safa
Safari/Chrome unless you use `appearance: none` + custom arrow + `option {
background: #0a0a0a; color: #f8fafc }` — which still doesn't work in all br
browsers.

**Fix:** Either document this limitation clearly, or use `@radix-ui/react-s
`@radix-ui/react-select` / `headlessui` for a styleable combobox. The curre
current plan creates a false primitive.

---

### High-Priority Gaps (should fix)

#### 8. Elevation contrast too subtle for perception
`canvas` `#030303` → `surface` `#0a0a0a` → `raised` `#111111` — **7 hex ste
steps each**. On non-OLED screens, these are indistinguishable. Better Stac
Stack uses `#0d1117` → `#161b22` → `#21262d` (larger gaps).

**Recommendation:** Widen to at least `#030303` → `#0d0d0d` → `#1a1a1a` or
add subtle `border-border` on every elevation boundary (already planned).

#### 9. No "when to use which neutral" decision matrix
DESIGN.md says "pick one per context" but doesn't define contexts. Teams wi
will inconsistently mix warm/cool.

**Add to DESIGN.md:**
| Context | Text | Border | Background |
|---------|------|--------|------------|
| Primary content (cards, modals) | `text-foreground` / `text-heading` | `b
`border-border` | `bg-surface-base` |
| Navigation, chrome, data-dense UI | `text-muted-steel` / `text-frost` | `
`border-steel-border` | `bg-raised` |
| Form inputs, elevated panels | `text-heading` | `border-steel-border` | `
`bg-raised` |
| Gold-accent actions | `text-accent` | `border-accent/30` | `bg-accent/10`
`bg-accent/10` |

#### 10. Missing primitives needed by existing pages
| Primitive | Used by | Status |
|-----------|---------|--------|
| **Sheet/Slide-over** | Mobile nav, mystable filters | Listed missing, not
not built |
| **Table** | PedigreeTable, admin tables | Listed missing, not built |
| **Checkbox/Radio/Switch** | PurchaseFlow, KYC, login | Not in plan |
| **Label/FormField** | All forms (a11y) | Not in plan |
| **Dropdown/Menu** | NavBar user menu, filters | Not in plan |
| **Separator** | Modal footer, card dividers | Not in plan |
| **Toast/Alert** | Purchase flow, KYC status | Not in plan |

At minimum: **Sheet, Table, Checkbox, Label** should be in Phase 2 — they b
block the Phase 3/5 sweeps.

#### 11. `force-dynamic` pages + tokenization = layout shift risk
`marketplace/[id]/page.tsx` and `mystable/page.tsx` are `force-dynamic` (st
(streaming SSR). Tokenizing 60+ values per file without visual regression t
testing will almost certainly introduce layout shifts on first paint.

**Mitigation:** Add `@next/bundle-analyzer` or run `npm run build && npm ru
run start` and visually diff every page *before* committing sweep tasks. Co
Consider Playwright visual regression.

#### 12. `GlowPillButton` references `via-primary` — token may not exist
Task 11 replaces `via-primary` with `via-accent`. But `primary` as a Tailwi
Tailwind color token (mapping to `--brand-gold` or similar) must exist in c
current config. Verify `tailwind.config.ts` has `primary: "var(--brand-gold
"var(--brand-gold)"` or similar. If not, the current code is already broken
broken or uses arbitrary value `via-[#d4a964]`.

#### 13. `LogoCarousel` `next/image` fill + no width constraint
```tsx
<div style={{ height: logoHeight }}>  // no width!
  <Image fill style={{ objectFit: "contain" }} />
</div>
```
`fill` makes image `absolute` `inset-0`. Parent needs `relative` + explicit
explicit dimensions. Height is set but width is `auto` (0 for empty div). I
Images won't render.

**Fix:** Parent needs `min-w-[auto]` or calculate widths. Better: use `<img
`<img>` with `width`/`height` props + `object-fit: contain` — simpler for l
logos.

#### 14. Reduced motion not respected
`LogoCarousel` infinite animation, `Modal` fade-in, existing shimmer/fade-i
shimmer/fade-in — none check `prefers-reduced-motion`.

**Fix:** Wrap animations in `@media (prefers-reduced-motion: reduce) { anim
animation: none; }` in `globals.css`.

#### 15. `DESIGN.md` lint command — package may not exist
`npx -y @google/design.md lint DESIGN.md` — `@google/design.md` is not a kn
known npm package. Google's design token spec uses `design-tokens` format (
(JSON) + tools like `style-dictionary`, `token-transformer`. The YAML front
front-matter format in the plan is not standard.

**Fix:** Either remove the lint step, or use `style-dictionary` with a toke
token JSON file, or just validate manually.

---

### Minor Issues (nice to fix)

#### 16. Container `padding="default"` uses `px-6 md:px-8 lg:px-12` — incon
inconsistent with section gap
Section gap token = 80px (`py-20`). Container horizontal padding at lg = 48
48px (`px-12`). These should relate (e.g., container padding = section gap
/ 2).

#### 17. Card `size="spacious"` = `p-8 md:p-10` but `default` = `p-6`. No `
`size="compact"` for dense UIs (admin, tables). Add `compact: "p-3"`.

#### 18. Modal `size="xl"` = `max-w-4xl` (896px). Marketplace detail modals
modals (investment terms) may need wider. Add `full: "max-w-[90vw] max-h-[9
max-h-[90vh]"`.

#### 19. Badge `variant="default"` uses `bg-white/[0.05]` — hardcoded opaci
opacity. Should use `bg-border` or new token `bg-badge-default`.

#### 20. Input/Select/Textarea don't share a base class — duplication. Extr
Extract `inputBaseClasses` constant.

#### 21. Task 5 (DESIGN.md) written before all tokens finalized — will drif
drift. Move to end of Phase 1 or make it a living doc updated per task.

#### 22. No TypeScript strictness check in CI — add `tsc --noEmit` to build
build verification steps.

#### 23. `ComingSoonOverlay` tokenization (Task 12) uses `bg-canvas/40` — `
`canvas` token maps to `#030303`. `bg-canvas/40` = `rgba(3,3,3,0.4)`. Origi
Original was `bg-black/40` = `rgba(0,0,0,0.4)`. Visually identical — good.
But verify `canvas` is in Tailwind config (Task 4).

#### 24. Phase 3 sweeps marketplace components *before* Table primitive exi
exists (Task 16 vs missing Table primitive). PedigreeTable tokenization wil
will be incomplete.

#### 25. `PressShowcaseSection` replacement (Task 18) — the original has pe
per-logo `opacity` adjustments (line 130-465). New `LogoCarousel` applies u
uniform `opacity-60 hover:opacity-90`. Brand team may want per-logo control
control.

---

### What the plan gets right

1. **Three-layer architecture** (tokens → primitives → composites) is corre
correct and matches shadcn/ui + Tailwind best practices.

2. **Keeping warm greyscale base + adding 4 cool neutrals as supplements**
— pragmatic. Don't rewrite the whole palette.

3. **Gold as single accent** — correct brand decision. Drop iris/periwinkle
iris/periwinkle.

4. **Container primitive with shared max-width** — solves the "every page g
guesses width" problem cleanly.

5. **Card/Header/Title/Description/Content/Footer composition** — follows s
shadcn pattern, familiar to React devs.

6. **Modal with Portal, backdrop blur, focus trap, Escape key** — productio
production-ready pattern.

7. **Tabs with Context + uncontrolled/controlled value** — correct React pa
pattern.

8. **Dot-grid utility separate from noise texture** — additive, not replace
replacement. Correct.

9. **Elevation system formalized as tokens** — even if contrast is subtle,
the *concept* enables future adjustment.

10. **CampaignStatusBadge / KycBadge from Badge primitive** — codifies Evo-
Evo-specific patterns correctly.

11. **Login page rebuild using primitives** — high-impact user-facing fix,
good use of the system.

12. **Sandbox deletion** — dead code removal before sweep is smart.

13. **Incremental commit-per-file** — reviewable, bisectable, safe.

14. **Final audit regex** — measurable definition of "done".

15. **Deferred items explicitly listed** — prevents scope creep, acknowledg
acknowledges real issues (perf, IA) without blocking design system work.

---

### Recommended Execution Order Adjustments

| Current | Adjusted | Reason |
|---------|----------|--------|
| Task 2 (Tailwind config) | **Merge into Task 1** | Single commit: tokens
+ config together |
| Task 4 (elevation tokens) | **Merge into Task 1** | Same file, same commi
commit |
| Task 3 (dot-grid) | **After Task 1** | Independent, low risk |
| Task 5 (DESIGN.md) | **Move to end of Phase 1** | Documents finalized tok
tokens |
| **New Task 2.5** | **Add status color tokens** | Unblocks Badge primitive
primitive (Task 7) |
| **New Task 2.6** | **Add Button accent variant** | Unblocks login rebuild
rebuild (Task 21.5) |
| **New Task 7.5** | **Build Sheet, Table, Checkbox, Label primitives** | U
Unblocks Phase 3/5 sweeps |
| Task 10 (Tabs) | **Before Task 16 (DetailTabs sweep)** | DetailTabs → Tab
Tabs primitive migration |
| Task 17 (LogoCarousel) | **Fix variable-width loop before Task 18** | Pre
Prevents homepage visual bug |
| Task 21.5 (login) | **After all primitives exist** | Depends on Card, Inp
Input, Button(accent) |
| Phase 5 sweeps | **Add visual regression check per file** | Catches layou
layout shifts on force-dynamic pages |

---

### Summary

**The plan is 80% there.** The architecture, token philosophy, and componen
component breakdown are sound. But **4 build-breaking issues** (missing sta
status colors, Button accent variant, Tailwind config syntax, Select primit
primitive limitations) and **3 accessibility/visual bugs** (contrast, LogoC
LogoCarousel loop, reduced motion) must be fixed before any code is written
written.

**Estimated fix effort:** ~2-3 hours to patch the plan, then execution proc
proceeds smoothly.

